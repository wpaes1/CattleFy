import type { Body, Meta, UppyFile, UppyFileId } from '@uppy/utils';
import type { AddFilePayload } from './ServiceWorker.js';
export type ServiceWorkerStoredFile<M extends Meta, B extends Body> = {
    type: string;
    store: string;
    file: UppyFile<M, B>;
};
type ServiceWorkerStoreOptions = {
    storeName: string;
};
declare class ServiceWorkerStore {
    #private;
    name: string;
    static isSupported: boolean;
    constructor(opts: ServiceWorkerStoreOptions);
    get ready(): Promise<void>;
    list(): Promise<Record<UppyFileId, Blob>>;
    put(file: AddFilePayload): Promise<void>;
    delete(fileID: UppyFileId): Promise<void>;
}
export default ServiceWorkerStore;
//# sourceMappingURL=ServiceWorkerStore.d.ts.map