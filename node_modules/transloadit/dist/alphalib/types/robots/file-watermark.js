import { z } from 'zod';
import { interpolateRobot, robotBase, robotUse } from "./_instructions-primitives.js";
// @ts-expect-error - FileWatermarkRobot is not ready yet @TODO please supply missing keys
export const meta = {
    name: 'FileWatermarkRobot',
    priceFactor: 4,
    queueSlotCount: 20,
    isAllowedForUrlTransform: true,
    trackOutputFileSize: false,
    isInternal: false,
    removeJobResultFilesFromDiskRightAfterStoringOnS3: false,
    stage: 'ga',
};
export const robotFileWatermarkInstructionsSchema = robotBase
    .merge(robotUse)
    .extend({
    robot: z.literal('/file/watermark'),
    randomize: z.boolean().optional(),
})
    .strict();
export const robotFileWatermarkInstructionsWithHiddenFieldsSchema = robotFileWatermarkInstructionsSchema.extend({
    result: z
        .union([z.literal('debug'), robotFileWatermarkInstructionsSchema.shape.result])
        .optional(),
});
export const interpolatableRobotFileWatermarkInstructionsSchema = interpolateRobot(robotFileWatermarkInstructionsSchema);
export const interpolatableRobotFileWatermarkInstructionsWithHiddenFieldsSchema = interpolateRobot(robotFileWatermarkInstructionsWithHiddenFieldsSchema);
//# sourceMappingURL=file-watermark.js.map