import { z } from 'zod';
import { interpolateRobot, robotBase, robotUse } from "./_instructions-primitives.js";
// @ts-expect-error - ProgressSimulateRobot is not ready yet @TODO please supply missing keys
export const meta = {
    name: 'ProgressSimulateRobot',
    priceFactor: 1,
    queueSlotCount: 20,
    isAllowedForUrlTransform: false,
    trackOutputFileSize: true,
    isInternal: true,
    stage: 'ga',
    removeJobResultFilesFromDiskRightAfterStoringOnS3: false,
};
export const robotProgressSimulateInstructionsSchema = robotBase
    .merge(robotUse)
    .extend({
    robot: z.literal('/progress/simulate'),
    duration: z.number(),
    output_files: z.number(),
    emit_progress: z.boolean(),
    predict_output: z.boolean(),
})
    .strict();
export const interpolatableRobotProgressSimulateInstructionsSchema = interpolateRobot(robotProgressSimulateInstructionsSchema);
//# sourceMappingURL=progress-simulate.js.map